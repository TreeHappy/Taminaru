.DS_Store
*.swp
*.tmp
*.lock.toml.bak
Thumbs.db

# carapace writes generated bin shims into ~/.config/carapace/bin at apply
# time; that path is excluded via dotfiles/.chezmoiignore, so nothing is
# tracked in the repo for it.